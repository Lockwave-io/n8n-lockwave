services:
  n8n:
    image: n8nio/n8n:latest
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
      - .:/home/node/.n8n/custom/node_modules/n8n-nodes-lockwave
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678

volumes:
  n8n_data:
